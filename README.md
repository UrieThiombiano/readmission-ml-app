# Readmission ML App (Streamlit)
- Upload → Préprocessing → Modèles (GridSearch) → Inference
- Multi-pages: `mlapp/app.py` (main), `mlapp/pages/...`

## Lancer en local
```bash
pip install -r requirements.txt
streamlit run mlapp/app.py
